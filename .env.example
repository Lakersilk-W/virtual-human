# Copy to .env and fill in real values (never commit .env)
# 包含特殊字符(&, ?, #, 空格)的值必须用引号包裹,
# 否则 `source .env` 会报 parse error

DEEPSEEK_API_KEY="sk-xxxxxxxxxxxxxxxxxxxxxxxx"

# ↓↓↓ 下面这些都是可选的,不填则走 application.yml 里的默认值
# (默认值已经匹配 docker-compose.yml, 本地开发不用改)

# MYSQL_URL="jdbc:mysql://localhost:3306/vh?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8&allowPublicKeyRetrieval=true"
# MYSQL_USER="vh"
# MYSQL_PASSWORD="vhpass"

# REDIS_HOST="localhost"
# REDIS_PORT="6379"

# MILVUS_HOST="localhost"
# MILVUS_PORT="19530"
